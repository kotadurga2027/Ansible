- name: play level variables
  hosts: local
  connection: local
  vars:
    COURSE: AWS
    DURATION: 120 hrs
    TRAINER: Sivakumar
  tasks:
  - name: print course information
    vars:
      COURSE: ansible
    ansible.builtin.debug:
      msg: "Hello i was joined in {{COURSE}} and duration is {{DURATION}} and trainer is {{TRAINER}}"
  - name: print course information again
    ansible.builtin.debug:
      msg: "Hello i was joined in {{COURSE}} and duration is {{DURATION}} and trainer is {{TRAINER}}"
